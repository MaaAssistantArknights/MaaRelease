name: update-resource

on:
  push:
    paths:
      - .github/workflows/update_resource.yml
  workflow_dispatch:

jobs:
  s3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: lts/*
          check-latest: true
      - name: Fire the webhook of MAA_S3
        if: github.repository == 'MaaAssistantArknights/MaaRelease'
        run: |
          node scripts/AnnAngela/webhookForJenkins.js
        env:
          WEBHOOK_URL: https://jenkins.maa-org.net/job/maa-resource-s3-sync/buildWithParameters?token=${{ secrets.MAA_JENKINS_PROJECT_TOKEN }}
          WEBHOOK_SECRET: ${{ secrets.MAA_JENKINS_ROBOT_TOKEN }}

  R2:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout MaaRelease
        uses: actions/checkout@v4
        with:
          show-progress: false
          path: ${{ runner.temp }}/MaaRelease
      - name: Setup workspace
        run: |
          mv ${{ runner.temp }}/MaaRelease ~/MaaRelease
          sh ~/MaaRelease/scripts/update_resource/updateResource.sh
      - name: Upload to R2
        if: github.repository == 'MaaAssistantArknights/MaaRelease'
        uses: ./.github/s3Sync
        continue-on-error: true
        with:
          args: --acl public-read --follow-symlinks
          AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-1
          AWS_S3_ENDPOINT: ${{ secrets.AWS_S3_ENDPOINT }}
          SOURCE_DIR: ~/MaaAssistantArknights/resource
          DEST_DIR: MaaAssistantArknights/MaaAssistantArknights/resource

  ota:
    runs-on: ubuntu-latest
    needs: 
      - s3
      - R2

    steps:
      - name: Setup ssh key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.OTA_SERVER_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" >> ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -t rsa -H ${{ secrets.OTA_SERVER_SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Update Resource
        run: |
          ssh ${{ secrets.OTA_SERVER_SSH_USER }}@${{ secrets.OTA_SERVER_SSH_HOST }} \
            "
              set -e
              sh ~/MaaRelease/scripts/update_resource/updateResource.sh
            "
